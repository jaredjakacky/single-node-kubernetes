---
# renovate: datasource=github-releases depName=kubernetes/kubernetes versioning=semver
kubernetes_version: "1.36.4"

# The selected upstream release is published as Debian revision 1.1.
kubernetes_package_version: "{{ kubernetes_version }}-1.1"
